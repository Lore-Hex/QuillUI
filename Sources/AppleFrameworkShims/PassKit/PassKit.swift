//
// QuillUI Linux shim for `PassKit` — placeholder so `import PassKit` resolves on
// Linux. Concrete symbols are added as SignalServiceKit references surface
// (behavior deferred). Part of the Signal-iOS -> QuillOS port.
//
import Foundation
