// Empty translation unit so SwiftPM treats `Sources/IOKit` as
// a buildable C target. The actual IOKit compatibility surface
// lives in the header files (IOKit.h, usb.h) referenced by the
// module.modulemap.
