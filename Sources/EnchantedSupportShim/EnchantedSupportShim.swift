// EnchantedSupportShim
// ====================
// Side declarations from upstream Enchanted's `@main` file
// (`Application/EnchantedApp.swift`) — extensions on
// KeyboardShortcuts.Name, etc. — are auto-extracted at build time by
// QuillMainExtractPlugin. See MainExtractInputs.txt in this directory
// for the list of upstream `@main` files we lift declarations from.
//
// Color/UIColor/NSColor/ShapeStyle asset symbols come from
// QuillAssetSymbolsPlugin (applied to the EnchantedUpstream target).
//
// Auto-imported into EnchantedUpstream via `-Xfrontend -import-module`,
// so upstream Enchanted source compiles unmodified.

// (Body intentionally empty — content is supplied by the plugin's
// generated `EnchantedApp.MainStripped.swift`.)
