You are an autonomous coding agent implementing GitHub issue #9 for the Lore-Hex/QuillUI repository.

Your working directory IS a fresh git worktree at this path. Implement the issue's acceptance criteria. When you're done, commit your work to the current branch (gemini/issue-9). Do NOT push or open a PR — the spawn wrapper will handle that.

Constraints:
- This is a Swift package (Package.swift at the repo root). Use the existing target structure.
- Do not modify Package.swift's existing target list unless the issue explicitly requires a new target.
- Do not touch other targets unless the issue scope requires it.
- The strict Mac-reference verifier targets pixel-perfect macOS appearance via the QuillPaint stack. Don't propose Adwaita or native-Linux feel.
- Write tests when the issue asks for them. Use Swift Testing (`import Testing`) and `@Test` / `@Suite` macros.
- Commit early, commit often. Multiple small commits are better than one big one.
- Final commit message should reference the issue: "Closes #9"

ISSUE TITLE: Generate quillui.dev coverage site from docs/apple-package-function-coverage.md

ISSUE BODY:
Roadmap item 8 — public coverage site.

The coverage matrix at docs/apple-package-function-coverage.md is the source of truth for QuillUI's compatibility surface. Right now an external developer who wants to know 'does QuillUI cover MapKit?' has to clone the repo and grep the markdown. A static HTML site fixes this.

Acceptance criteria:
- scripts/generate-coverage-site.py — Python 3, stdlib-only (or markdown-it-py if available, gracefully skip if not).
- Output: single self-contained HTML at docs/site/index.html — embedded CSS + JS, no CDN deps.
- Page structure: title + intro paragraph (extracted from the markdown's leading content); searchable table with columns module name, total rows, parity rows, usable rows, partial rows, compile-only rows; counts from maturity labels; module names anchor to rendered section below; client-side filter input; full markdown rendered as HTML below the table.
- scripts/test-generate-coverage-site.sh — runs the generator and grep-asserts output has title, table with >=5 rows, filter input, rendered markdown.
- Commit the initial generated docs/site/index.html (browsable directly on GitHub).
- Idempotent: re-running the script produces byte-identical output.

Hints:
- Maturity labels in the markdown: Parity, Usable, Partial, Fallback, Compile-only. Count instances per section.
- For stdlib-only markdown-to-HTML, a minimal regex-based pass handling headings, paragraphs, lists, code blocks, and links is enough.

Begin.
