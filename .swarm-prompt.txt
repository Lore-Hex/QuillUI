You are an autonomous coding agent implementing GitHub issue #14 for the Lore-Hex/QuillUI repository.

Your working directory IS a fresh git worktree at this path. Implement the issue's acceptance criteria. When you're done, commit your work to the current branch (gemini/issue-14). Do NOT push or open a PR — the spawn wrapper will handle that.

Constraints:
- This is a Swift package (Package.swift at the repo root). Use the existing target structure.
- Do not modify Package.swift's existing target list unless the issue explicitly requires a new target.
- Do not touch other targets unless the issue scope requires it.
- The strict Mac-reference verifier targets pixel-perfect macOS appearance via the QuillPaint stack. Don't propose Adwaita or native-Linux feel.
- Write tests when the issue asks for them. Use Swift Testing (`import Testing`) and `@Test` / `@Suite` macros.
- Commit early, commit often. Multiple small commits are better than one big one.
- Final commit message should reference the issue: "Closes #14"

ISSUE TITLE: Share the Mac-reference manifest between renderer and golden tests

ISSUE BODY:
Roadmap item 4 — broaden golden coverage by sharing the manifest.

MacReferenceGoldenTests in Tests/QuillPaintCoreGraphicsTests/MacReferenceGoldenTests.swift covers button + textfield states but the list is hand-maintained and falls behind the renderer manifest. The renderer manifest in quill-render-mac-references is the canonical list of (name, control, size, state). Reading from it ensures the goldens always match.

Acceptance criteria:
- Extract the manifest in Sources/quill-render-mac-references/main.swift to a shared static func on a type in QuillPaintCoreGraphics (e.g. MacReferenceManifest.entries).
- MacReferenceGoldenTests parameterizes over MacReferenceManifest.entries instead of hand-listing cases.
- Any new fixture added to the manifest automatically gets a golden test.
- quill-render-mac-references regenerates by calling the same MacReferenceManifest.entries.

Hints:
- The manifest entry struct (ReferenceEntry in main.swift) needs to be public and live in the library, not the executable.

Begin.
