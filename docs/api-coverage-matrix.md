# QuillUI API Coverage Matrix

Coverage labels mean:

- `compile`: source imports and representative calls compile on Linux.
- `behavior`: the compatibility layer performs useful runtime behavior.
- `native`: the behavior is backed by Linux desktop services or GTK instead of an in-memory or diagnostic fallback.
- `tested`: regression coverage exists in `Tests/`.

| Surface | Compile | Behavior | Native | Tested | Current Evidence | Next Fix |
| --- | --- | --- | --- | --- | --- | --- |
| SwiftUI facade | yes | partial-real | partial-real | yes | `SwiftUI` re-exports QuillUI on Linux; `FocusState`, `Image(nsImage:)`, `Window`, core modifiers, generated Enchanted shared chat components, and `ImageRenderer` compile; `ImageRenderer` rasterizes solid `Color` content to real PNG bytes through gdk-pixbuf by default, `PlatformImage` scales valid bitmap bytes by requested height and recompresses to JPEG, and the opt-in GTK/Xvfb offscreen snapshot pipeline can rasterize arbitrary SwiftUI-shaped view trees. | Broaden offscreen snapshot coverage across composed Enchanted views and reduce the opt-in risk before enabling by default. |
| Layout/navigation | yes | partial | partial-real | yes | `NavigationSplitView`, stacks, scroll, forms, sheets, environment shims, `LazyVGrid`, `GridItem`, list-row source modifiers, reusable `QuillDesktopSplitLayout`, `QuillToolbarActionRow`, and `QuillSidebarNavigationButton` compile through SwiftOpenUI/QuillUI; `listRowInsets` and `listRowSeparator` now preserve row layout/separator metadata with diagnostics. The generic SwiftOpenUI GTK patch now widens split-view sidebars, propagates expansion through `Group`/multi-child wrappers, flattens single-item lazy-grid tuple content, uses a static GTK grid path for small finite lazy grids, fixes finite `maxWidth` expansion for expandable children, renders original-source `NavigationSplitView` detail toolbars without the Enchanted `ChatView` replacement template, and lets strict visual QA capture a full `2048x1380` frame. Visual parity is still not close enough: the Mac reference screenshot measures `2228x1498` with a `602px` sidebar, `102px` header, four prompt cards, a `1524px` alert, and a `1510px` composer; current original-source Linux output reaches the full frame but fails the strict sidebar divider check at `x=471`, `ratio=0.230`, `score=1`, with prompt-card state and alert state still remaining. | Replace patch-script renderer surgery with an allocation-aware SwiftUI layout backend, then fix split-view column painting/allocation, animation/onAppear settling, and state-backed alert/composer parity against the Mac-reference visual verifier. |
| Menus/toolbars/commands | yes | partial-real | partial | yes | Command/menu builders extract labels/actions/shortcuts, preserve disabled state for command items, suppress disabled menu actions, expose SPI-tested view-tree extraction helpers, and provide `QuillToolbarIconButton` plus `QuillToolbarMenuButton` for macOS-like icon toolbar affordances with QuillUI popover actions under GTK. SwiftOpenUI `AnyToolbarItem` is patched to preserve composite toolbar children after type erasure, so `ToolbarItemGroup` content from original Enchanted source renders instead of requiring a generated toolbar template; duplicate detail toolbar installation into window chrome is suppressed for split-view details. | Replace the temporary GTK toolbar-row positioning fallback with allocation-aware native GTK/libadwaita toolbar layout and add click-outside/keyboard navigation behavior to QuillUI popovers. |
| Forms/input styles | yes | partial-real | partial | yes | Picker and text field styles compile; grouped form styles render with visible grouped padding/background; `textContentType`, `disableAutocorrection`, `keyboardType`, and `autocapitalization` now preserve text-input metadata with diagnostics. | Make preserved text-input metadata affect GTK and Qt controls instead of remaining diagnostic-only. |
| File/drop APIs | yes | partial-real | partial | yes | `NSItemProvider`, `UTType`, `.fileImporter`, and `.onDrop` have tested file/data behavior, extension inference, content-type validation, and concrete SwiftOpenUI wrapper return types on Linux. | Replace command-dialog fallback with native GTK picker when available. |
| Visual effects/animation | yes | partial-real | partial | yes | Gradients reduce to visible colors; `AngularGradient` maps to a visible color fallback; materials have visible fallbacks; `strokeBorder(style:)` and shape masks compile; masks map to `clipShape`; generic `View.imageScale`, `minimumScaleFactor`, and `contentShape` preserve metadata with diagnostics; `symbolEffect`, matched geometry, transitions, hover, focus, text selection, `Animation.snappy`, `repeatForever`, and `delay` compile with diagnostics where behavior is still fallback. | Implement exact GTK-backed symbol, transition, focus, and matched-geometry effects where possible. |
| `@AppStorage` | yes | yes | yes | yes | Values persist through `UserDefaults` on Linux and bindings write through. | Add suite-isolated stores if tests need more process isolation. |
| QuillData/SwiftData shape | partial | partial-real | yes | yes | SQLite JSON-row store supports insert/fetch/delete/sort/value-predicate/upsert/relationships, `hasChanges`, stable `id`/`name`/`slug` identity, class identity-map saves, class-safe `QuillPredicate`, optional wrapper defaults, and generated source lowering for `@Model`/`@Transient`/class `#Predicate`; Enchanted's real SwiftData model/service core now compiles as generated Linux source. | Promote source lowering into a macro/build plugin, then add schema/indexing, migrations, relationship delete rules, and `@Observable`/actor-hop lowering. |
| Build-time SwiftUI lowering | yes | yes | n/a | yes | `scripts/lower-swiftui-source-for-linux.sh` performs app-agnostic generated-copy rewrites for `@main`, `@Observable`, preview blocks, `@MainActor`, `View, Sendable`, and positive `#if os(macOS)` gates by widening them to `(os(macOS) || os(Linux))` while preserving negated fallback checks. This keeps the upstream app tree unchanged while allowing Linux to compile the desktop source path. | Replace regex lowering with a SwiftSyntax package command/build plugin so nested conditions and diagnostics are structured instead of text-only. |
| Combine | yes | mostly | mostly | yes | Linux `Combine` facade re-exports OpenCombine plus small compatibility shims for `AnyPublisher()`, `NotificationCenter.publisher`, and `Publishers.Merge`. | Audit IceCubes/NetNewsWire Combine usage against OpenCombine operators before adding local shims. |
| Foundation | yes | yes | yes | yes | Linux uses Swift's official Foundation implementation via normal `import Foundation`; `QuillParityTests` run the same URL, UserDefaults, JSON, Calendar, and QuillData assertions on macOS and Linux, with portable-range expectations where swift-corelibs differs from Apple Foundation. | Prefer Foundation APIs over custom shims whenever available. |
| Apple platform services / QuillKit | partial | partial-real | partial | yes | AppKit/UIKit/AVFoundation/Speech/Security/CoreGraphics/ServiceManagement/Carbon/IOKit/os modules compile and record diagnostics or explicit unavailable state; AppKit exposes QuillUI's Linux `NSImage`, `NSBitmapImageRep`, `NSWindow`, one-shot hotkey fallback, drawing/compositing placeholders, gdk-pixbuf-backed `NSImage.tiffRepresentation` transcoding for PNG/JPEG/GIF/BMP/WebP input, `PlatformImage` height scaling and JPEG recompression for valid bitmap data, and opt-in GTK-backed `ImageRenderer` pixels for arbitrary SwiftUI-shaped content. QuillKit centralizes reusable platform capability reporting, diagnostics, clipboard memory fallback, URL opening through `xdg-open`, speech fallback state, accessibility/panel/updater/device placeholders, image fallback services, a process-local `KeychainSwift` compatibility store, and explicit capability markers for secure storage, notifications, network extensions, and VPN tunnels. | Add native Linux backends for clipboard, speech, shortcuts, key events, secret storage, updater, launch-at-login, USB/device events, and WireGuard-style network-extension/service-management needs. |
| Third-party UI packages | partial | partial-real | partial | yes | ActivityIndicatorView, MarkdownUI, Splash, Vortex, WrappingHStack, AsyncAlgorithms, Alamofire, and OllamaKit module shims compile; OllamaKit performs real HTTP-backed model, reachability, and streaming-chat requests, including the Quill Chat `session:` initializer shape; MarkdownUI/Splash cover Enchanted's theme, code-block, relative spacing, table, and syntax-highlighter contracts. | Add richer Markdown block rendering and audit the next target app's package imports. |
| Enchanted/Quill Chat app | partial-real | partial | partial | yes | Quill-inspired app and upstream-shaped slice run under GTK; generated Linux harnesses compile 87/87 real upstream Enchanted Swift files and the local 92-file Quill Chat rebrand through QuillUI/QuillData/QuillKit without editing app sources; the generated Qt entry now links `QuillGenericQtNativeRuntime` instead of falling through the GTK runtime registry. The large `ChatView_macOS`, empty-state, and sidebar-button templates have been removed, leaving a small profile alias file plus rewrite/empty-file metadata. `scripts/build-swiftui-linux-app.sh` is the generic app-build entry point, `scripts/generate-swiftui-linux-package.sh` is the reusable generated-package builder, and `scripts/build-quill-chat-linux.sh` is now a thin Quill Chat wrapper over the `enchanted-full-source` profile. | Finish exact visual parity for original-source `LazyVGrid` cards/composer and replace service fallbacks with native Linux behavior, then promote profile lowering into SwiftPM plugin/package-command infrastructure. |
| QA scripts | yes | yes | n/a | shell | Linux backend smoke checks test, patch the pinned SwiftOpenUI checkout for GTK CSS and Quill Chat icon-map gaps, generated Enchanted core, generated Enchanted chat components, generated Enchanted macOS chat, generated Enchanted full-source, optional local Quill Chat generated app build through the generic builder, reusable generated-package assembly, builds, Xvfb app survival using SwiftPM's active bin path, and cross-platform parity tests; final smoke greps for missing Material-symbol and GTK `object-fit` regressions; backend visual smoke checks screenshot brightness and variation for backend launch fixtures, root products, and generated `quill-chat-linux`, with generated app rows now requested through both GTK and Qt; visual and interaction smoke runners share `quillui-linux-backend-smoke-lib.sh` for Linux package setup, executable resolution, and Quill Chat reference-data seeding; generated Quill Chat screenshots assert compact sidebar/header/toolbar/prompt-card/composer landmarks; `QUILLUI_BACKEND_MAC_REFERENCE=1` enables a strict Mac-reference verifier for the supplied Quill Chat screenshot while the legacy `QUILLUI_GTK_MAC_REFERENCE=1` name remains supported; backend interaction smoke clicks deterministic QuillUI GTK and Qt launch samples plus the generated Quill Chat toolbar menu across the generated app backend matrix with `xdotool`; GitHub Actions runs Linux tests, generated Enchanted compile, visual smoke, and both interaction smokes with screenshot/log artifacts. | Add perceptual screenshot comparisons and CI-friendly package caching once the strict Mac-reference verifier is passing. |
